import { createAction } from "typesafe-actions";
export const appInitialised = createAction("APP_INITIALISED");
export const getPagesInit = createAction("GET_PAGES_INIT");
export const getPostsInit = createAction("GET_POSTS_INIT");
